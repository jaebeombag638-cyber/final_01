# 로컬 개발: Docker PostgreSQL
DATABASE_URL=postgresql+psycopg://buildback:buildback@localhost:5432/buildback

# 테스트 DB (운영 DB와 분리)
TEST_DATABASE_URL=postgresql+psycopg://buildback:buildback@localhost:5432/buildback_test
